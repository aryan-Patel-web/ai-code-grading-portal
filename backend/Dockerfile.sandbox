# Dockerfile.sandbox
# Used ONLY to run student-submitted code in isolation.
# At runtime: docker run --rm --network=none --memory=128m --cpus=0.5 -i python:3.11-slim python -
FROM python:3.11-slim
WORKDIR /sandbox
CMD ["python", "--version"]
